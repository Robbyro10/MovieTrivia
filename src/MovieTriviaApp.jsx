import { useState } from "react";
import reactLogo from "./assets/react.svg";

export const MovieTriviaApp = () => {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1>Hello world</h1>
    </div>
  );
};
